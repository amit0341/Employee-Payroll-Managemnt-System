CREATE TABLE deductions (`SNo` INT NOT NULL AUTO_INCREMENT, firstname VARCHAR(255) NOT NULL, lastname VARCHAR(255) NOT NULL, salary INT NOT NULL, deduction_amount INT NOT NULL, deduction_reason VARCHAR(255) NOT NULL, `Emp_ID` INT NOT NULL, PRIMARY KEY (`SNo`));
